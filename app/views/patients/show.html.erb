<h2><%= @patient.name %></h2>

<h4>Appointments</h4>
<ul>
    <% @patient.appointments.each do |app| %>
        <li><%= app.appointment_datetime.strftime('%B %d, %Y at %H:%M') %> with <%= link_to app.doctor.name, doctor_path(app.doctor) %></li>
    <% end %>
</ul>
