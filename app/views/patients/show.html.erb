<h1>Patient: <%= @patient.name %></h1>


<ul>
    <% @patient.appointments.each do |appt| %>
        <li><%= link_to appt.doctor_name, doctor_path(appt.doctor) %> <%= link_to appt.formatted_date, appointment_path(appt) %></li>
    <% end %>
</ul>